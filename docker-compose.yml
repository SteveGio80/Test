services:
  lightdash:
    image: lightdash/lightdash:latest
    environment:
      DATABASE_HOST: ep-autumn-sea-aeyv34za-pooler.c-2.us-east-2.aws.neon.tech
      DATABASE_PORT: 5432
      DATABASE_NAME: neondb
      DATABASE_USERNAME: neondb_owner
      DATABASE_PASSWORD: Phen0mSauce19002!
      DATABASE_SSL: "true"
      LIGHTDASH_SECRET: supersecretkey123
      LIGHTDASH_PORT: 8080
    ports:
      - "8080:8080"
    volumes:
      - /home/itadmin/my_dbt_project:/dbt
    restart: unless-stopped
