name: 'my_dbt_project'
version: '1.0.0'

profile: 'my_dbt_project'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  my_dbt_project:
    +materialized: view

seeds:
  my_dbt_project:
    slm_data:
      alias: slm_data_seed
    phenom_domains:
      alias: phenom_domains_seed
